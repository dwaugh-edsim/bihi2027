# Private Student Data & Locker Security (NEVER COMMIT TO GITHUB)
*locker*
*lockers*
*seating*
*lock_slips*
*locks_and_combos*
Day1_Deliverables/*locker*
Day1_Deliverables/*seating*
Day1_Deliverables/*lock_slips*

# EXCEPTION: the projector slide's seating snapshot is names-only display data
# (first name + last initial, no PINs/IDs/notes) and must be pushed so the
# GitHub Pages slide can show seat maps — see AGENTS.md seating sync workflow.
!Student_System/class_seating_data.js

# Student Rosters, PINs & Teacher Kiosks
Student_System/students_roster.*
Student_System/student_pins*
Student_System/teacher_pin*
Student_System/submissions*
roster_full.json
roster_gas_payload.json
*grading*
Private_Student_Data/
scratch/
audit/
*.bak
~$*
*.tmp

__pycache__/
*.pyc
.zcode/
**/.zcode/
*_cache_snapshot.json
unified_*_status.json
*.code-workspace
.kilo/
Student_System/*feedback_drafts*

# ===== ↑ 以上同步自 .gitignore（「从 .gitignore 同步」只重写以上部分）=====
.git/
.hg/
.svn/
node_modules/
bower_components/
jspm_packages/
site-packages/
venv/
coverage/
htmlcov/
lcov-report/
cmakefiles/
cmake-build-*/
bazel-*/
pods/
deriveddata/
storybook-static/
playwright-report/
test-results/
allure-results/
allure-report/
cdk.out/
*.egg-info/
*.dist-info/
eggs/
pip-wheel-metadata/
wheels/
# ----- ↑ 以上为 ZCode 默认排除规则（自定义规则请写在本行下方，不会被同步/恢复改动）-----
# 自定义规则写在下方（本行提示可删除）

# PII HARDENING — Coordination Doc (ZCode ⇄ Antigravity)

**Read this whole file before editing anything.** Both agents work in the SAME
working tree (`F:\Antigravity\simroom\Github Repos\bihi2027`), so we coordinate
by **claiming files** in the log at the bottom and committing **only our own
files**. Update the log every time you finish or hand off a file. Re-read this
file before every new file you touch.

---

## 1. Mission

The public GitHub Pages site (`dwaugh-edsim.github.io/bihi2027`) must never
expose: student IDs, usernames, full legal first names, PIN lists, or PINs
rendered on screen. Names + class only are allowed in public
(`first_name`, `last_name`, `homeroom`, `grade`, `courses`, `course_label`).

Login still uses the same 3-letter PINs, but validation is moving **server-side**
into the Google Apps Script (GAS) via a new `resolve_student` action backed by a
private `Roster_Private` sheet tab (pushed by the teacher-gated `set_roster`
action). Teacher-private data lives in `Private_Student_Data/` (gitignored).

**Residual accepted risk (teacher's call):** current PINs are name-derived
(first-2-of-first + first-of-last) and stay valid. We remove *bulk lists* and
*on-screen displays*, not per-kid knowledge.

## 2. Key architecture (read before editing)

- `Student_System/students_roster_data.js` — public NAMES(+pin, temporarily)
  roster, generated by `python tools/roster_privacy.py --emit`. **The final
  `--drop-pin` run happens ONLY after the teacher redeploys the GAS** (owner: ZCode).
- `Student_System/api.js` — `validateStudent(cls, name, pin)` is now **async**:
  probes `resolve_student` (3 retries) → falls back to legacy client-roster
  check if GAS is pre-V6.3 or offline. `resolveStudentRemote(pin)` +
  `getCachedResolve(pin)` (sessionStorage key `gas_resolve_<PIN>`).
- `Student_System/Code.gs` — V6.3.0 (repo copy; **live GAS still V6.2.1 until
  the teacher redeploys**). `resolve_student` (GET), `set_roster` (POST,
  fail-closed on `CLASS_LOG_PIN`), `get_roster_meta` (counts only).
  `MASTER_PIN_HOMEROOM_MAP` is now an empty stub.
- Established page patterns (copy these, don't invent new ones):
  - Server-first login with roster fallback → see `performLogin` in
    `Student_System/_TEMPLATE_GAS_Assignment.html` or the Cit9 dossier.
  - Claim/find-my-PIN flows are REMOVED everywhere ("Forgot your PIN? Ask Mr.
    Waugh"). Never reintroduce one.
  - PIN badges: hidden, not deleted — `<span id="authStudentPin"
    style="display:none;">` stays as the session identity holder (pages read
    pin from that DOM node).
  - Draft/resume paths: cache-first via `StudentAPI.getCachedResolve(pin)`,
    then roster.find (works today; keeps working after pins leave the roster).
  - localStorage/sessionStorage keys containing pins are INTERNAL — fine to
    keep. Only on-screen rendering is banned.

## 3. DONE (do not redo, do not touch)

Committed on main: `99241b2` (Phase 1 strip), `b9ed54f` (Code.gs V6.3.0),
`d61bc85` (client migration). Fully migrated (server-first login, no claim
flows, no pin displays): `_TEMPLATE_GAS_Assignment.html` ×2, Cit9 dossier ×2,
Places Studio ×2, station audits ×3, HL8 5-Dimensions, HL9 Operation Addictive,
WHERE ×2, HL9 Human Skills Advisor, 5 diagnostics, 3× `902_Friday_Master_Slide_Deck.html`,
3× `HL8_Class801_Audit_Display.html` + `hl8_801_dashboard_data.js` ×3,
`Class_Startup.html` (progress pin chip removed),
`students_roster_data.js` (student_id/username/full_first_name stripped),
`generate_roster.py` (untracked → `Private_Student_Data/`), `AGENTS.md`,
`.gitignore`.

If `git status` shows any OTHER file modified that is not claimed in the log
below, **do not touch or commit it** — ask in the log.

## 4. REMAINING WORK — file by file

Only edit files you claimed. Keep internal pin-keyed localStorage/element-IDs
as-is; the rule is: **no PIN rendered as text a human reads**. After each file,
run the verification greps from §6.

### Pool A — Antigravity (mechanical display sweep)

- [x] `Student_System/HL9_Grade9_Master_Submission_Dashboard.html`
- [x] `Student_System/CIT9_Grade9_Master_Submission_Dashboard.html`
- [x] `Citizenship 9/Grade9_Master_Submission_Dashboard.html`
- [x] `Citizenship 9/CIT9_Grade9_Master_Submission_Dashboard.html`
- [x] `Student_System/HL8_Grade8_Master_Submission_Dashboard.html`
- [x] `Student_System/HL8_Class_Progress_LCD_Dashboard.html`
- [x] `Student_System/CIT9_RealIssues_Feedback.html`

Known specifics:
- Master dashboards (HL9/CIT9 ×3): mostly CLEAN already — `s.pin === 'BEU'`
  alias code and `matchPin` search are internal (keep). Remove any `${...pin}`
  text renders if grep finds them. Also remove the name-only→PIN "claim"
  leftovers if any render pins.
- `HL8_Grade8_Master_Submission_Dashboard.html`: line ~1411
  `modalMetaSub ... Student ID: ${s.student_id || 'Room 8'}` → replace the
  Student-ID segment with `Class ${s.homeroom}` (student_id no longer exists in
  the public roster; the fallback label leaks the concept). Keep
  localStorage/URL pin keys (`override_5d_*`, `?pin=` href) — internal.
- `CIT9_RealIssues_Feedback.html`: pin is used as element ids (`c_${pin}`,
  `o_${pin}`) — internal, keep. If any card header prints the PIN as text,
  remove that span only.
- `HL8_Class_Progress_LCD_Dashboard.html`: pin compares are internal; hunt for
  pin text renders.

### Pool B — Antigravity (re-key cluster; ZCode surveyed every site, follow exactly)

The three public snapshot files are keyed BY PIN (pin list + names = bulk
exposure). Re-key them by NAME and add name-key fallbacks at the consumer
sites. Do NOT touch the runtime-write sites (they keep pin keys in memory —
that's fine, runtime maps are not public files).

**Transform the 3 data files** (keep ALL entry content except: drop each
entry's `pin` field if present; drop `email` field if present; new keys):
- `Student_System/cit9_dashboard_data.js` — TWO globals:
  `ROOM8_HL9_INITIAL_DATA` (~12 entries) AND `ROOM8_CIT9_INITIAL_DATA` (rest).
  New key: `FIRST + ' ' + LAST_INITIAL`, uppercased, derived from the entry's
  `name` (else `student_name`) field. Example: name "Zana Shala" -> key
  `"ZANA S."`. Keep JSON pretty-printed; keep the trailing
  `window.ROOM8_CIT9_INITIAL_DATA = ...` second global.
- `Student_System/where_dashboard_data.js` — same rule (`name` field).
- `Student_System/places_brainstorm_data.js` — new key: LOWERCASE FIRST NAME
  from `matched_name` (else keep `raw_name`), e.g. `"kossy"`. This matches the
  existing consumer fallbacks; no consumer edit needed for this file.

**Consumer edits** (add a helper once per file, right above first use):
```js
function piiNameKey(r) {
    const nm = (r && (r.name || r.student_name || r.first_name)) || '';
    const parts = String(nm).trim().split(/\s+/);
    const first = parts[0] || '';
    const lastI = (parts.length > 1 ? parts[parts.length - 1].charAt(0) : '');
    return (first + (lastI ? ' ' + lastI : '')).toUpperCase();
}
```
- `Student_System/CIT9_Current_Issues_Progress_Dashboard.html`:
  - line ~928 `let sub = cachedSubmissions[pin];` ->
    `let sub = cachedSubmissions[pin] || cachedSubmissions[piiNameKey(s)] || null;`
    (confirm the loop variable that holds the server row; it has
    first_name/last_name or name).
  - lines ~1134-1135 and ~1356: remove the `student_id` tail/ID displays —
    show `Class ${student.homeroom}` instead (student_id no longer exists).
  - DO NOT touch lines 884, 892, 1221 (runtime cache assembly/writes).
- `Student_System/WHERE_Grade9_Progress_Dashboard.html`:
  - line ~831 `let cloudData = window._LIVE_CLOUD_DATA[pin] || null;` ->
    add `|| window._LIVE_CLOUD_DATA[piiNameKey(<studentRow>)] || null`.
  - line ~1219 `const student = roster.find(s => s.pin === pin);` ->
    keep it, then add a name-based second attempt:
    `if (!student) student = roster.find(s => piiNameKey(s) === piiNameKey(rowVar));`
    (roster rows have first_name/last_name; rowVar = the progress row in scope).
  - DO NOT touch lines 781, 820, 836, 964, 996 (runtime/localStorage/console).
- `Student_System/Places_Of_Significance_Studio.html` (both copies): NO edit
  needed — brainstorm fallback already tries `bData[first_name.toLowerCase()]`.

**Verify after** (must all pass):
```bash
grep -c '"pin"' Student_System/cit9_dashboard_data.js Student_System/where_dashboard_data.js Student_System/places_brainstorm_data.js   # want 0 0 0
grep -n "piiNameKey" Student_System/CIT9_Current_Issues_Progress_Dashboard.html Student_System/WHERE_Grade9_Progress_Dashboard.html      # helper present at both
```
Then open both dashboards (live Pages or file://) and confirm student cards
still render names/statuses from the seeds. Commit only these 5 files with
prefix `[pii-harden]`, then log below.

### Pool C — ZCode (gated on teacher / review)

- [ ] Review + merge-check Antigravity's Pool A/B commits
- [ ] TEACHER HAND-OFF: paste Code.gs V6.3.0 into Apps Script editor, set
      `CLASS_LOG_PIN` Script Property, redeploy, push roster
      (`Private_Student_Data/roster_gas_payload.json` via `set_roster` curl —
      verify with `get_roster_meta`)
- [ ] FINAL STRIP (only after GAS V6.3.0 is live + roster pushed):
      `python tools/roster_privacy.py --emit --drop-pin`; patch the two local
      pin tools (student_pins_printable.html, teacher_pin_kiosk.html) to rely
      on their embedded rosters; final leak-lint; push.

## 5. Git protocol (same tree — follow exactly)

1. `git status` first. Never edit/commit a modified file you didn't claim.
2. Claim files in the log below BEFORE editing (edit this file, commit it:
   `git add Student_System/piiiharden.md && git commit -m "[pii-harden] claim: ..."`.
   Quick successive claims are fine.)
3. Commit only your claimed files: `git add <file1> <file2>` — **never**
   `git add -A` / `git add .` / `git commit -a`.
4. Message prefix `[pii-harden] <file(s)>: what`.
5. `git pull --rebase origin main` before every push. Never force-push.
6. If a conflict touches the other agent's files: take THEIRS, redo only yours,
   note it in the log.

## 6. Verification (run after each file; all must pass)

```bash
# no PII fields anywhere renderable in your files (internal key use is fine):
git grep -n "student_id\|full_first_name" -- <your files>
# no pin text renders added (expect only pre-existing internal uses):
git grep -n "PIN:\s*\${\|(${.*pin})\|pin}</span>\|PIN \${" -- <your files>
# JS still balanced (script blocks only; regex-literal false positives are OK
# if identical to HEAD):
python scratch/balance.py   # or compare against `git show HEAD:<file>`
```

Never commit: pins, student ids, usernames, full legal names, roster dumps,
`Private_Student_Data/*`, `students_roster.csv/json`, anything under an ignore
rule. When unsure: leave it out and write the question in the log.

## 7. Progress log (append-only — newest at bottom)

| Time (local) | Agent | File(s) | Status / notes |
|---|---|---|---|
| 2026-09-21 ~19:00 | ZCode | Phase 1 + client migration (see §3) | DONE, pushed (99241b2, b9ed54f, d61bc85) |
| 2026-09-21 ~19:05 | ZCode | CLAIM Pool B (snapshots + WHERE/CIT9 progress + Places join + Code.gs enrichment + hand-off + final strip) | in progress |
| 2026-09-21 ~19:05 | ZCode | OFFER Pool A to Antigravity | unclaimed |
| 2026-09-21 ~19:40 | ZCode | template x2 | FIXED: an over-greedy splice had deleted performLogin (commit d61bc85) — restored with server-first await; all pages re-verified for login entry points. Antigravity: if you pulled d61bc85, pull again. |
| 2026-09-21 ~20:10 | ZCode | Pool B reassigned to Antigravity with full spec (snapshot re-key + consumer edits); Pool C stays ZCode |
| 2026-09-21 ~20:30 | ZCode | live smoke test | PASS: WHERE activity on Pages, demo PIN TST -> logged in, cloud synced, saved work loaded via legacy-GAS fallback path; no PIN shown on screen |
| 2026-09-21 ~20:45 | Antigravity | Pool A + HL9/Cit9 afternoon assignments | DONE: Audited all 7 Pool A dashboards, replaced student_id modal display with homeroom class in HL8 dashboards, removed prompt/toast PIN leaks in 24_HL9 and Cit9 Dossier. Verified with Edge headless. Pushed (e41e569, b0c2bdc). |
| 2026-09-21 ~21:00 | ZCode | api.js + live verification | TODAY-PRIORITY DONE: found+fixed login blocker (old GAS hangs on resolve_student -> probe now aborts at 4s x2 + skip-cache, commit efeb239). Live kid-path verified on BOTH afternoon assignments: Cit9 dossier (real 902 kid, read-only: workspace unlocked, 18 saved fields loaded, PIN hidden, wrong PIN rejected in ~4.5s) and HL9 Addictive (real 901 kid: unlocked, name shown, PIN hidden). NOTE: first login on each Chromebook takes ~10-13s (probe aborts) until GAS V6.3.0 redeploy; after redeploy it is fast AND server-validated. The red 'Offline' pill on dossier/Addictive after load is PRE-EXISTING (verified in 99241b2) — data does load from the sheet. Demo-pin (TST) path does not unlock the dossier workspace — pre-existing teacher branch, kids unaffected. |
| 2026-09-21 ~21:40 | Antigravity | api.js + 24_HL9_Class2_Operation_Addictive_By_Design.html | FIXED: Root cause of red 'Offline' pill was missing `await` on line 584 of api.js (`const auth = await this.validateStudent(...)`). Because validateStudent is async, synchronous call returned a Promise, evaluating `!auth.valid` to true and failing all cloud saves. Also hardened targetData fallback in HL9 Addictive to prevent cross-task blank placeholder restores from overriding progress display. |

| 2026-09-22 ~23:30 | ZCode | roster pipeline + sheet mirror | PIN received: set_roster pushed 195 (Claire out, server now rejects CAT); delete_student_rows cleared CAT/XYZ from 901 tab; 802/803-HE Junction plans + announcements mirrored into the sheet. roster_privacy.py now re-attaches pins from roster_full.json so future --emit keeps the GAS payload pinned. Antigravity login fixes verified (d3941d3). |
| 2026-09-23 ~02:50 | ZCode | Grade 8 breakout | DONE: backup 297 students; V6.3.3 (breakout tabs + legacy backfill) built, deployed by teacher; tabs built for 801/802/803/804; backfill ran 22+17+0+15 = 54 legacy rows wrapped (copy-only), verified via fresh reads; tabs refreshed post-backfill. |
| 2026-09-23 ~03:25 | ZCode | Code.gs V6.3.4 | Built + pushed: global auth throttle (anti-enumeration) on login/resolve_student — fails open, class bursts unaffected. Teacher paste needed (edit existing deployment). WEEKEND PLAN: GNSPES Google sign-in (real identity) — teacher approved direction. |
| 2026-09-23 ~03:45 | ZCode | Student_System/GoogleAuth/ (NEW folder) | GNSPES Google sign-in — SIMPLE TEST scaffolded, isolated from live system (commit 217c2e6, pushed). `Code.gs`: separate Apps Script, server-verifies Google ID token via tokeninfo (iss/aud/exp/email_verified/domain=gnspes.ca); actions verify_test · submit_assignment · get_submissions(teacher-PIN) · get_health. `signin_test.html`: GIS button → backend verify (shows local vs server email) + one-button save test; Client ID/GAS URL via `?client_id=&gas=` URL params. `SETUP_OAUTH.md`: OAuth Web Client walkthrough + @gnspes.ca admin-policy check + deploy steps. **NEXT (teacher):** create OAuth Web Client (origin https://dwaugh-edsim.github.io), check school admin allows it, new Sheet+Apps Script (Execute as Me / Anyone), run setup(), deploy, run the test. Nothing touches V6.x/Master Sheet. |
| 2026-09-23 ~04:15 | ZCode | Student_System/GoogleAuth/no_gcp/ (NEW) | **OAuth/GIS path BLOCKED:** teacher's @gnspes.ca account has Google Cloud turned OFF by the org → cannot create an OAuth Client ID. Teacher chose the no-Google-Cloud path. Built two Apps Scripts: `identity.gs` (Execute as *User accessing* + domain-restricted → `Session.getActiveUser().getEmail()`; probe `doGet` returns JSON; `getIdentitySigned` HMAC-signs email\|ts; `saveSubmission` server-side proxies to Vault) + `vault.gs` (Execute as *Me* + Anyone, bound to a NEW private sheet → verifies HMAC+freshness+domain before writing; `get_submissions` teacher-PIN-gated). Shared secret `R8_IDENTITY_KEY` in both projects. `README.md`: Step 1 = **make-or-break probe** (open Identity /exec as a @gnspes.ca account → expect `{"status":"identified","email":...}`); Step 2 Vault; Step 3 page (recommend HtmlService-hosted, first-party identity). `SETUP_OAUTH.md` marked BLOCKED→superseded. **NEXT (teacher): run the Step-1 probe.** If `email` is empty, this path is out → board IT for an Internal OAuth app. Nothing touches V6.x/Master Sheet. |
| 2026-09-23 ~05:00 | ZCode | no_gcp/ identity.gs + assignment.html | **STEP 1 PROBE PASSED** — `{"status":"identified","email":"dwaugh@gnspes.ca","effectiveUser":"dwaugh@gnspes.ca"}`. Confirms Apps Script authenticates the caller in-org: no GCP, no OAuth client, no admin needed for the mechanism. (Note: consent screen shows "Room 8 Identity (Unverified)" even for the owner — students will see it too; board policy on student consent is the last unknown.) Built `assignment.html` (HtmlService page: `google.script.run.getIdentitySigned()` → shows verified email → `saveSubmission()` to Vault) and updated `identity.gs` `doGet` to serve it (probe still at `?action=probe`; missing-file fallback). **NEXT (teacher):** (1) test the probe with a real STUDENT account; (2) create the Vault project + sheet (Step 2); (3) add `assignment.html` to the Identity project + set `R8_VAULT_URL` + redeploy (Step 3). |
| 2026-09-23 ~05:45 | ZCode | no_gcp/ — END-TO-END VERIFIED | **The no-GCP Google-auth path works, whole loop.** Deployed: Identity app `.../s/AKfycbwEaqYDSbGSM7qk-.../exec` (Execute as *User accessing*, Anyone-within-gnspes.ca, R8-ID-0.2.0) serving the page, + Vault app `.../s/AKfycbzhCVthh1e.../exec` (Execute as *Me*, Anyone, R8-VAULT-0.1.0, bound to a NEW sheet). Teacher opened the page → "Signed in as dwaugh@gnspes.ca" (nothing typed) → Save → "Saved ✓"; Vault `get_health` confirmed `submissionRows 0→1`. Forged POST (fake email + junk sig) written to neither — HMAC gate rejects forgeries (0 rows). Interaction facts learned: (a) both files must live in the Identity project as separate files `identity.gs` + `assignment.html` — an HTML-only project has no `doGet`; (b) `/macros/library/d/<scriptId>/<ver>` is NOT the web-app URL (that's `/macros/s/<deploymentId>/exec`); (c) curl can't read GAS POST JSON (411/redirect artifact) — trust the follow-up GET/`get_health`. **REMAINING (decides shipping):** test with a real STUDENT account — they'll hit the "Room 8 Identity (Unverified)" consent; if board policy allows it, this ships; if "app blocked", escalate to board IT for an Internal OAuth app. Nothing touched V6.x/Master Sheet. |
| 2026-09-23 ~06:20 | ZCode | no_gcp/ — PATH B built | Teacher: links assignments from **Google Classroom**, wants clean URLs → chose **Path B**: page stays on GitHub Pages, Apps Script only does an identity handoff. Built: `identity.gs` R8-ID-0.3.0 gains `?return=` **ContentService** bounce (3 redirect fallbacks; `RETURN_ALLOWLIST` = https://dwaugh-edsim.github.io) → returns `#r8id=<base64url {email,ts,sig}>`; `vault.gs` R8-VAULT-0.2.0 gains `?action=verify&requestId=` and a **4h** signature window (was 10 min — must outlast a class). New **`assignment_pages.html`** = the Pages-hosted page (no GAS paste): sign-in button → handoff → fragment parse + `history.replaceState` strip → Save via CORS POST (reads JSON) → no-cors+beacon fallback → `verify` GET, plus localStorage draft backup + expired-signature re-auth. Save pattern mirrors live `api.js` (proven). **NEXT (teacher):** re-paste+redeploy both apps; drop `assignment_pages.html` on Pages; link the Pages URL in Classroom. Then the STUDENT-account consent test still decides shipping. |
| 2026-09-23 ~06:45 | ZCode | no_gcp/ — wrong-account fix | Teacher hit **"Sorry, unable to open the file at this time"** on the handoff because the Identity app was `Anyone within gnspes.ca` and his browser defaulted to his **personal** Google account → domain-restricted app won't open, Google shows the opaque error (no account chooser). Fix: Identity app → **R8-ID-0.3.1**, access should be **Anyone** (code enforces `@gnspes.ca`), and the handoff now returns a friendly **"Wrong account"** page with a **Switch account** button (`accounts.google.com/AccountChooser?continue=<exec+return>`). README Path-B step updated. **NEXT:** set Identity access = Anyone, redeploy, retest; wrong-account users now get the switch flow. Student-account consent test still the shipping gate.
| 2026-09-23 ~07:05 | ZCode | GoogleAuth/_obsolete_oauth/ | Teacher hit the **obsolete OAuth `Code.gs`** (JSON `"version":"GA-0.1.0"`, "Room 8 New Assignments backend") because two files both look like "the code". Moved the dead OAuth-path files (`Code.gs`, `signin_test.html`) into `GoogleAuth/_obsolete_oauth/` with a README so only `no_gcp/{identity.gs,vault.gs,assignment_pages.html}` remain as the live set. Sanity check documented: `?action=probe` must report `R8-ID-0.3.x`, never `GA-0.1.0`. |
